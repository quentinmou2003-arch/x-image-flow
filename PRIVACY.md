# 隐私与权限说明

拾图在用户当前浏览的 X 页面提取媒体，通过 X 原生按钮执行用户选择的点赞、书签操作。原页面不再包含按钮时，扩展会临时打开对应推文标签页，完成操作后关闭它。

扩展没有独立服务器、广告或分析统计服务，不会把浏览记录上传到项目维护者。图片和拼接处理在本地浏览器执行；加载图片、视频或执行账号操作仍会与 X 的服务通信。

## 请求的权限

| 权限 | 用途 |
| --- | --- |
| activeTab | 用户点击扩展图标时操作当前标签页 |
| clipboardWrite | 点击复制按钮后写入 PNG 剪贴板 |
| offscreen | 在后台文档中解码图片、拼接、生成 PNG |
| downloads | 用户点击保存后下载文件 |
| storage | 保存图片密度、预取等偏好 |
| pbs.twimg.com | 获取 X 原尺寸图片 |
| X / Twitter 页面内容脚本 | 收集媒体、展示图库、响应用户操作 |

喜欢与书签的页面跳转使用标签页会话存储，帮助在跳转后重新打开图库。图库内容和处理缓存主要保存在运行中的内存里。扩展不读取 QQ 会话，也不会替用户发送 QQ 消息。
